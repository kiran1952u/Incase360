-- SQL Script to insert notice tracking data
-- Range: IN3542-157 to IN3542-75156
-- Table: tracking_status (adjust table and column names as needed)

-- Adjust the table name and column names according to your database schema
-- Example structure:
-- CREATE TABLE tracking_status (
--     notice_id VARCHAR(50),
--     barcode VARCHAR(50),
--     status VARCHAR(100),
--     status_date DATE,
--     post_office VARCHAR(200),
--     delivery_status VARCHAR(100),
--     reason VARCHAR(500),
--     service_status VARCHAR(50),
--     remarks VARCHAR(100),
--     created_timestamp DATETIME
-- );

-- Bulk INSERT using VALUES
-- Batch size: 1000 records per INSERT for optimal performance

